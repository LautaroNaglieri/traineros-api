/** Test de humo del esqueleto: el healthcheck responde 200. */
import { describe, it, expect } from 'vitest';

describe('healthcheck', () => {
  it('debería estar definido el endpoint /health', () => {
    // Placeholder del esqueleto: se reemplaza por un test de integración con supertest.
    expect(true).toBe(true);
  });
});
